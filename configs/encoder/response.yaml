variant: response
history_length: 8
future_length: 4
proprio_dim: 10
action_dim: 7
privileged_dim: 16
response_dim: 12
latent_dim: 32
response_embedding_dim: 32
hidden_dim: 64
use_rgbd: true
mask_goal_channel: true
image_channels: 4
image_size: 16
lambda_distill: 0.0
